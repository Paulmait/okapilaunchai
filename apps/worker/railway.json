{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd /app && corepack enable && pnpm install && pnpm -r --filter @okapilaunch/core --filter @okapilaunch/ai-router --filter @okapilaunch/worker build"
  },
  "deploy": {
    "startCommand": "cd /app/apps/worker && node dist/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
