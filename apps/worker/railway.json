{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": ["apps/worker/**", "packages/**"],
    "buildCommand": "corepack enable && pnpm install && pnpm --filter @okapilaunch/core build && pnpm --filter @okapilaunch/ai-router build && pnpm --filter @okapilaunch/worker build"
  },
  "deploy": {
    "startCommand": "node /app/apps/worker/dist/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
